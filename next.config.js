

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'shiftkey.se',
      'images.unsplash.com'
    ],
  }
}

module.exports = nextConfig
